spring.application.name=eduattend-pro

# Database Configuration: Neon PostgreSQL
# Primary connection string provided by user. Environment variables act as overrides for production.
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://ep-divine-silence-a80qhgl1-pooler.eastus2.azure.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_Lh1IVf2PKkDq}
spring.datasource.driverClassName=${DATABASE_DRIVER:org.postgresql.Driver}

# JPA / Hibernate Settings
# 'update' will automatically create tables in your Neon database based on the Entity classes.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

# Static Content Configuration
spring.web.resources.static-locations=classpath:/static/

# Server Port Configuration
server.port=${PORT:8080}

# H2 Console (Disabled since we are now using a live PostgreSQL database)
spring.h2.console.enabled=false
